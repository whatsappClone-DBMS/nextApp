import nc from "next-connect";
import {deleteMessage } from "../../../controller/chats";

function Handler(req, res) {
//   const Handler = nc();
//   const { dmId, mId } = req.query;
//   if (dmId && mId) {
//     Handler.get(deleteMessage(req, res, dmId, mId));
//   }
//   return Handler;
}

export default Handler;